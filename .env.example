# URL del BFF (backend PHP activo). Cambiar en producción.
VITE_API_BASE_URL=http://localhost:3001

# Supabase (clave pública/anon — segura para frontend)
# La anon key se usa solo con Row Level Security (RLS) activo.
# Operaciones sensibles (guardar notas, configs) van a través del BFF, no directo.
VITE_SUPABASE_URL=https://yiwunoxlibblgoyhakur.supabase.co
VITE_SUPABASE_ANON_KEY=

# NOTA: La SUPABASE_SERVICE_ROLE_KEY y la DATABASE_URL van SOLO en backend/.env
# Nunca expongas claves privadas en el frontend.
